import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
    paddingTop: 120,
    alignItems: 'center'
  },
  logoStyles: {
    width: 240,
    height: 100
  }
});

export default styles;